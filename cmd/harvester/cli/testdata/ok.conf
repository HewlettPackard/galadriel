harvester {
    local_socket_path = "/tmp/galadriel-harvester/api.sock"
    spire_socket_path = "/tmp/api.sock"
    server_address = "localhost:5000"
    server_trust_bundle_path = "./root_ca.crt"
    federated_bundles_poll_interval = "1m"
    log_level = "DEBUG"
}
